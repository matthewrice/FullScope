var React = require('react');
var AppHeader = require('./app-header.jsx');


/*
 * I didn't have time to work on this view.  This component would've allowed the
 * Supporter to support a Patient Family's need.
 */
var SupportPatientFamily = React.createClass({

  render: function(){

    return (
      <div className="row">
        // <AppHeader />

      </div>
    );
  }
});


module.exports = SupportPatientFamily;
